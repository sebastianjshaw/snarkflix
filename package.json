{
  "name": "snarkflix",
  "version": "1.0.0",
  "description": "A snarky movie review blog with sharp wit and adorable Bernedoodle companion",
  "main": "index.html",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "serve": "node server.js",
    "serve:python": "python3 -m http.server 8000",
    "build": "node generate-review-pages.js && node generate-sitemap.js && node generate-image-sitemap.js",
    "optimize-images": "node optimize-images.js",
    "compress-images": "bash compress-images-advanced.sh",
    "update-version": "node update-version.js",
    "deploy": "npm run update-version && npm run build && git add -A && echo 'Files staged. Ready to commit and push!'"
  },
  "keywords": [
    "movie-reviews",
    "blog",
    "cinema",
    "entertainment",
    "snarky",
    "reviews"
  ],
  "author": "Snarkflix",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/dom": "^9.3.3",
    "jsdom": "^23.0.1"
  }
}
